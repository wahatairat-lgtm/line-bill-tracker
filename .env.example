# ───── LINE Messaging API channel ─────
LINE_CHANNEL_ACCESS_TOKEN=
LINE_CHANNEL_SECRET=

# ───── Anthropic (Claude OCR) ─────
ANTHROPIC_API_KEY=

# ───── PostgreSQL (Render ใส่ให้อัตโนมัติ) ─────
DATABASE_URL=

# ───── Cron reminder (ดู README หัวข้อ cron) ─────
CRON_SECRET=

# ───── Local dev only ─────
PORT=3000
